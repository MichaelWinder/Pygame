import pygame
import random
import time
pygame.init()

height = random.randrange(200, 1000, 20)
length = random.randrange(200, 1500, 20)
screen = pygame.display.set_mode((length, height))

time.sleep(99999)

# pygame.quit()
# quit()
